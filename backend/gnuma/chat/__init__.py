#
# This file contains the consumers needed by Quipu in order to handle the notifications, and the instant messages systems.
# Also, it contains all the RESTful endpoints which are meant to create resources onto the server.
#
# models.py: contains all the Django models needed by the chat system.ù
# serializers.py: contains all the serializers.
# rest.py: contains all the Django REST endpoints.
# routing.py: contains the routing rules used by ASGI.
# notifications.py: contains all the consumers used by the notification system.
# chat.py: contains all the consumers used by the chat system.
#